#!/usr/bin/make -f

# Needed to be compatible with Ubuntu 13.04 release of libsbml5
DEB_CONFIGURE_EXTRA_FLAGS = --enable-cpp-namespace

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk
